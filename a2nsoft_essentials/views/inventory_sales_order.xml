<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="custom_sales_order_inventory_form" model="ir.ui.view">
            <field name="name">custom.sales.order.inventory.form</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>

            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="is_inventeriod" invisible="1"/>
                </xpath>

                <xpath expr="//field[@name='partner_invoice_id']" position="attributes">
                   <attribute name="attrs">{'invisible':[('is_inventeriod','=',False)]}</attribute>
                </xpath>
                <xpath expr="//field[@name='partner_shipping_id']" position="attributes">
                    <attribute name="attrs">{'invisible':[('is_inventeriod','=',False)]}</attribute>
                </xpath>

            </field>
        </record>

</odoo>